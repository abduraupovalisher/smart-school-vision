[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short -m "not live"
markers =
    live: tests that require a physical camera pointed at a running server (opt-in; skipped by default)
